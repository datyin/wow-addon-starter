local T, L, P = unpack(select(2, ...))

-- Default Settings
T.Settings = {
  profile = {
    message = "Welcome Home!",
    showInChat = false,
    font = nil,
    fontOutline = "NONE",
    texture = nil,
    sound = "None",
  },
}
